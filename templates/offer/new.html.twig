{% extends 'base.html.twig' %}

{% block title %}New Offer
{% endblock %}

{% block body %}
	<main id="main" class="main text-center">
		<div class="container d-flex justify-content-center align-items-center">
			<div class="row w-100">
				<div class="">

					<h1>Create new Offer</h1>

					{{ include('offer/_form.html.twig') }}

					<a href="{{ path('app_offer_index') }}" class="btn btn-primary mt-3">Back to list</a>
				</main>
			</div>
		</div>
	</div>

{% endblock %}
