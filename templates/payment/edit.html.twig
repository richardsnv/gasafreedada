{# {% extends 'base.html.twig' %}

{% block title %}Edit Payment
{% endblock %}

{% block body %}
	<main id="main" class="main ">

		<div class="row">
			<div class="col-2"></div>
			<div class="col-6">
				<h1>Edit Payment</h1>


				{{ include('payment/_form.html.twig', {'button_label': 'Update'}) }}

				<a href="{{ path('app_payment_index') }}" class="btn btn-primary mt-3">back to list</a>

				{{ include('payment/_delete_form.html.twig') }}

			</div>
		</div>
	</div>
{% endblock %} #}
