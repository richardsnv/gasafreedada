{% extends 'base.html.twig' %}

{% block title %}Log in!
{% endblock %}

{% block body %}

	<main id="main" class="main ">
		<div class="container">
			<div class="row">
				<div class="col-md-12">
		
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<a href="{{ path('app_login') }}" class="btn btn-primary mt-3">{{ button_label|default('Enregistrer') }}</a>

		{# <a href="{{ path('app_login') }}"><button class="btn btn-primary mt-3">{{ button_label|default('Enregister') }}</button></a> #}
		{{ form_end(form) }}
					</div>
			</div>
		</div>
	</main>
{% endblock %}
